#include <vector>

using namespace std;

class Solution {
public:
int max = nums[0];
    int maxElement(vector<int>& nums) {
        for(int i=0;i<nums.size()i++){
            if(nums[i]>max){
                max =num[i]
            }
            
        }
    
    }
    return max;
};
